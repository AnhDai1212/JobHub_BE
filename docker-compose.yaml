services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    networks:
      - jobhub-net

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: "Jobhub123@"
      MYSQL_DATABASE: job_hub
      MYSQL_USER: jobhub
      MYSQL_PASSWORD: "123ABC@"
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - jobhub-net

  app:
    build: .
    image: tuadai/datn-app:1.15
    ports:
      - "${APP_PORT:-8080}:8080"
    env_file:
      - .env
    depends_on:
      - mysql
      - redis
    networks:
      - jobhub-net
  cv-parser:
    build: ./cv-parser-service
    image: tuadai/datn-cv-parser:1.12
    ports:
      - "8000:8000"
    networks:
      - jobhub-net
networks:
  jobhub-net:

volumes:
  mysql_data:
